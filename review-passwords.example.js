// review-passwords.example.js -- commit this; copy to review-passwords.js (gitignored) and fill in hashes.
// Map of SHA-256(password) -> reviewer_name
// To generate a hash: python -c "import hashlib; print(hashlib.sha256(b'PASSWORD').hexdigest())"
window.REVIEW_PASSWORDS = {
  // 'sha256hashhere': 'reviewername',
};
